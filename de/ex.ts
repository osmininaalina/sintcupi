// Define a tuple type that represents the structure of the array
type MixedArray = [string, number, boolean];

// Initialize the array with the tuple type annotation
let anArr: MixedArray = ["hello1", 234, true];
